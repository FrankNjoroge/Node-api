import getPostsLength, { getPosts } from "./postController.js";

// const { generateRandomNumber, celciusToFahrenheight } = require("./utils");
// console.log(`RandomNumber:`, generateRandomNumber());
// console.log(`Fahrenheight:`, celciusToFahrenheight(0));

console.log(getPostsLength());
